# --- Web framework ---
fastapi
uvicorn[standard]
python-multipart

# --- Database ---
motor
pymongo
dnspython

# --- Config ---
python-dotenv

# --- Auth ---
passlib[bcrypt]
bcrypt==4.0.1  # passlib 1.7.4 is unmaintained and breaks on bcrypt>=4.1
PyJWT

# --- Gemini (quiz + chapter generation) ---
google-generativeai

# --- YouTube ingestion ---
yt-dlp
pydub
audioop-lts; python_version >= "3.13"  # pydub needs stdlib audioop, removed in Python 3.13

# --- ML pipeline (transcription, summarization, translation) ---
torch
transformers==4.57.1  # transformers 5.x removed the "summarization"/"translation" pipeline tasks this code relies on
sentencepiece
sacremoses
numpy
